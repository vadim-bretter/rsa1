скачиваем архив.
1.заходим в папку bigint статической библиотеки которая будет подключаться при сборки самой программы.
2.открываем файл bigint.pro
3.собираем проект библиотеки.
4.после сборки библиотеки(bigint.lib),запускаем проект самой программы RSA.pro.
5.собираем проект RSA.